# Copy to .env.local and adjust as needed:
#   copy .env.local.example .env.local

WELLLOG_API_URL=http://localhost:8000

# Prosper AI explanations (server-side only — do not use NEXT_PUBLIC_)
ANTHROPIC_API_KEY=sk-ant-...
