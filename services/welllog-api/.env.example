# Required — get a key at https://console.groq.com
GROQ_API_KEY=gsk_...

# Optional — store analysis history
# DATABASE_URL=postgresql://user:pass@localhost:5432/learntodig

# Comma-separated origins allowed to call the API directly (bypassing Next proxy)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173
